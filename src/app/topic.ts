export class Topic {

    name: string;
    draftText: string;
    sentText: string;

    constructor(name, draftText, sentText){
        this.name = name;
        this.draftText = draftText;
        this.sentText = sentText;
    }

}
