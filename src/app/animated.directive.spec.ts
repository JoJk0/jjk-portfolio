import { AnimatedDirective } from './animated.directive';

describe('AnimatedDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedDirective();
    expect(directive).toBeTruthy();
  });
});
